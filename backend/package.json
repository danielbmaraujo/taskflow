{
  "name": "taskflow-backend",
  "version": "1.0.0",
  "description": "TaskFlow REST API with Express",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "cypress run --config-file ../cypress.config.js --spec '../cypress/e2e/backend.cy.js'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
